{{- if .Values.serviceAccount.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "o11y-canary.fullname" . }}-sa
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "o11y-canary.labels" . | nindent 4 }}
{{- end }}
